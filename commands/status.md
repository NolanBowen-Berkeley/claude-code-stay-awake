---
description: Show Stay Awake state (assertion, background watchdog, config)
allowed-tools: Bash(sh "*/scripts/stay-awake.sh" status)
---
Stay Awake status (captured just now):

```
!`sh "${CLAUDE_PLUGIN_ROOT}/scripts/stay-awake.sh" status`
```

Show the status block above to the user exactly as-is inside a code block, followed by at most two plain sentences interpreting it. Do not run any tools.
